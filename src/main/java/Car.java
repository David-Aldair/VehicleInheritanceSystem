public class Car extends Vehicle{

    //car super constructor
    public Car(String color, int numOfPassengers, int cargoCapacity, int fuelCapacity) {
        super(color, numOfPassengers, cargoCapacity, fuelCapacity);
    }
}
